#include <stdio.h>
#include <stdlib.h>
#include <unistd.h> //Para fork()
#include <errno.h>
#include <sys/wait.h>

int main(int argc, char const *argv[])
        int c;
        int status;
        pid_t pid, pid2;

        printf( mi pid, get pidd) //  1000
        pid= fork() //se crean dos hilos   el 1001 y el 0
        //(es el hijo)
        if(pid==0){
            pid2=fork(); //1002 en el hijo y el 0
            char * ruta= getenv("HOME"); 
            if(pid2==0){    // --------> nos permite ir al proceso nuevo
                
                    //
                    // LO  QUE QUIERAS METER
                    //
            }
            pid= wait(NULL);
            while (pid !=-1){
            pid = wait(NULL);
        }   
{  
        }



        
        pid= wait(NULL);
        while (pid !=-1){
            pid = wait(NULL);
        }   
{  